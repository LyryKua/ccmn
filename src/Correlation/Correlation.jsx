import React, { Component } from 'react';

class Correlation extends Component {
    render() {
        return (
            <div>
                Correlation
            </div>
        );
    }
}

export default Correlation;
